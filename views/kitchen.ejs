<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Kitchen</title>
        <link rel="stylesheet" href="/styles.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Ubuntu+Mono:ital,wght@0,400;0,700;1,400;1,700&display=swap" rel="stylesheet">
    </head>
<body>

    <nav id="navbar" class="navbar" style="padding: 10px 10px;">
        <div style="display: flex; flex-direction: row; align-items: center;">
            <a href="/employee" style="margin-left: 10px; margin-right: 20px;"><img src="/img/management/arrow-left.png" class="back-arrow"></a>
            <div>
                <p1 style="font-weight: 500;">Employees</p1>
                <h1 style="font-size: 50px; font-weight: 600; margin-top: -15px;">Kitchen</h1>
            </div>
        </div>
        <ul class="nav-links">
            <li><a href="javascript:void(0)" onclick="openHelpBox()" style="display: flex; align-items: center; gap: 8px; text-decoration: none; color: black;">
                <img src="/img/management/question.png" style="height: 40px;">
            </a></li>
        </ul>
    </nav>

    <div class="modal" id="help-modal">
        <!-- Modal box -->
        <div id="modal-box">
            <h3 style="margin-bottom: 16px;">Help</h3>

            <p>Select an order card to complete that order. If the customer has provided a phone number,
                they will be sent a text notifying them that their order is ready.
            </p>

            <div style="display: flex; justify-content: center; gap: 16px; margin-top: 20px;">
                <button onclick="closeHelpBox()" style="padding: 8px 16px; border: 1px solid #ccc; background: white; border-radius: 6px; cursor: pointer;">
                    Close
                </button>
            </div>
        </div>
    </div>

    <div class="modal" id="complete-modal">
        <!-- Modal box -->
        <div id="modal-box">
            <h3 id="complete-modal-title" style="margin-bottom: 16px;"></h3>

            <p>Completing this order will notify the customer that their order is ready.</p>

            <div style="display: flex; justify-content: center; gap: 16px; margin-top: 20px;">
                <button id="completeButton" style="padding: 8px 16px; border: none; background: #d9534f; color: white; border-radius: 6px; cursor: pointer;">
                    Complete
                </button>
                <button onclick="closeCompleteBox()" style="padding: 8px 16px; border: 1px solid #ccc; background: white; border-radius: 6px; cursor: pointer;">
                    Cancel
                </button>
            </div>
        </div>
    </div>
    
    <section style="overflow-y: auto; height: 100%;">
        <!-- Main body of page -->
        <% if (incompleteOrders && incompleteOrders.length > 0) { %>
        <div class="grid">
            <% incompleteOrders.forEach(order => { %>
                <div class="card" style="height: auto;" onclick="openCompleteBox(<%= order.order_id %>)">
                    <h3>Order #<%- order.order_id %></h3>
                    <p style="margin-bottom: 20px;">Created: <%- order.date_time %></p>
                    <ul>
                        <% order.items.forEach(item => { %>
                        <li>
                            Product: <%- item.product_name %><br>
                            <% if (item.addon_string) { %>
                                - <%- item.addon_string%>
                            <% } %>
                        </li>
                        <% }) %>
                    </ul>
                </div>
            <% }); %>
        <div style="height: 50px;"></div>        
        <% } else { %>
            <div style="height: 100%; width: 100%; text-align: center; align-items: center; align-content: center;">
                <h3>There are no orders in progress right now.</p>
            </div>
            
        <% } %>
        
    </section>
    
    <script type="module" src="/langSelect.js"></script>
    <script src="/index.js"></script>
    <script src="/accountDropdown.js"></script>
    <script src="/weather.js"></script>
    <script src="/kitchen.js"></script>
</body>
</html>